<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlanetoApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowFullscreen"/>
        <item name="android:windowTranslucentNavigation"/>
        <item name="android:windowTranslucentStatus"/>

    </style>

    <style name="Theme.PlanetoApp" parent="Base.Theme.PlanetoApp" />
</resources>